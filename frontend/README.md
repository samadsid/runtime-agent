# Customer web chat

Copy `.env.example` to `.env`, install dependencies with `npm install`, then run
`npm run dev`. The FastAPI server must allow the exact Vite origin. Browser
configuration is public; never place credentials in `VITE_*` variables.

Use `npm test`, `npm run test:e2e`, and `npm run build` for validation.
